package com.cuello.token;

public enum TokenType {
    ILLEGAL,
    EOF,
    IDENTIFIER,
    INT,
    ASSIGN,
    PLUS,
    COMMA,
    SEMICOLON,
    LPAREN,
    RPAREN,
    LBRACE,
    RBRACE,
    FUNCTION,
    LET,
    NOT,
    MINUS,
    DIVIDE,
    MULTIPLY,
    LESS_THAN,
    GREATER_THAN,
    IF,
    ELSE,
    RETURN,
    TRUE,
    FALSE,
    EQUAL,
    NOT_EQUAL
}
